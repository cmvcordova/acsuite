# @package _global_
tags: ["ACNet", "siamese_nss", "regression", "pretraining"]

defaults:
  - override /model: representation_learning/siamese
  - override /data: representation_learning/ECFP_R4_ac_mixed
  - override /hydra: pretrain_encoder
  - override /callbacks: regression

model_name: siamese_nss
task_type: regression
metric_direction: min
feature_length: 2048

model:
  net:
    out_features: 1
  input_type: pair

data:
  input_type: pair