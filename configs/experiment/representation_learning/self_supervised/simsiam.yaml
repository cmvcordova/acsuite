# @package _global_
tags: ["ACNet", "halfstep_joint_AE", "reconstruction", "pretraining"]

defaults:
  - override /model: representation_learning/joint_hse
  - override /data: representation_learning/ECFP_R4_ac_mixed
  - override /hydra: pretrain_encoder
  - override /callbacks: self_supervision

model_name: hse_btwins
task_type: self_supervision
metric_direction: min
feature_length: 1024

model:
  net:
    _target_: src.models.components.encoders.HalfStepBarlowTwins

data:
  input_type: pair