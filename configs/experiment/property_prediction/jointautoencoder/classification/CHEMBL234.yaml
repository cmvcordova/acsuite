# @package _global_

# to execute this experiment run:
# python train.py experiment=mlp_training/default

defaults:
- override /data: property_prediction/regression
- override /model: models/components/predictors/aca_mlp
- override /callbacks: default
- override /trainer: default

tags: ["chembl234 - classification"]

data: ## calls MoleculeACEDataModule directly
  dataset_name: ["CHEMBL234_Ki"]

model: ## calls ACAPPModule directly
  net:
    pretrained_autoencoder_ckpt: 'data/models/autoencoders/ae_test.ckpt'
  task: classification